var path=require("path"),fs=require("fs"),glob=require("glob"),browserify=require("browserify"),POSIX_SEP=path.posix?path.posix.sep:"/",ROOT_DIR=path.resolve(__dirname,".."),outputFile=path.resolve(ROOT_DIR,"test","browser","tests.js"),entries=glob(path.join(ROOT_DIR,"test","**","*-test.js"),{sync:!0});path.sep!==POSIX_SEP&&(entries=entries.map(function(e){return e.split(POSIX_SEP).join(path.sep)})),browserify({entries:entries}).external("buster").bundle().pipe(fs.createWriteStream(outputFile));